import HomeTemplate from '../components/Templates/Home';

export default function Home() {
  // Podemos usar Fetchings o logica de negocio (NO ES RECOMENDABLE)
  // Aca se suelen pasar los Context providers
  return <HomeTemplate />;
}
